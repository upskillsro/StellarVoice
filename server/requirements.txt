fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
requests>=2.32.0
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0
# Kokoro TTS
kokoro>=0.9.4
# NeMo ASR (Parakeet v3)
# Note: PyTorch must be installed first. See: https://pytorch.org/get-started/locally/
# Then: pip install nemo_toolkit[asr]
# If Parakeet cannot install, a whisper fallback will be used automatically.
nemo_toolkit[asr]
